[pytest]
DJANGO_SETTINGS_MODULE = config.settings
python_files = tests.py test_*.py *_tests.py

markers =
    unit: tests unitaires (logique isolée, sans client Django)
    integration: tests d’intégration (client Django, BD, sessions, messages, etc.)
